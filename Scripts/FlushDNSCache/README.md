# Flush DNS Cache

**Clears the macOS DNS cache.**

Restarts `mDNSResponder` and flushes the `dscacheutil` cache. Standard first-line remediation for name resolution problems, suitable for Self Service.
