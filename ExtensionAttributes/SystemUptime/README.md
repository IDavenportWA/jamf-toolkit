# System Uptime

Reports current system uptime.

Long uptime is a practical compliance signal: staged updates are not applied until a restart, so a Mac can report patched while still running the old kernel and frameworks.

Pairs with [RestartMac](../../Scripts/RestartMac), which escalates on the same measure.

**Data type:** String.
